<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/eq_title"
        android:textColor="@color/text_primary"
        android:textSize="17sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btnEqPreset"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/eq_tab_left"
            android:gravity="center"
            android:text="@string/eq_preset"
            android:textSize="14sp"
            android:textColor="@color/text_normal" />

        <TextView
            android:id="@+id/btnEqCustom"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/eq_tab_right"
            android:gravity="center"
            android:text="@string/eq_custom"
            android:textSize="14sp"
            android:textColor="@color/text_normal" />
    </LinearLayout>

    <com.example.subtitleplayer.EqualizerView
        android:id="@+id/equalizerView"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        android:layout_marginTop="14dp"
        android:background="@color/eq_panel_bg" />

    <TextView
        android:id="@+id/btnEqRestore"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="14dp"
        android:background="@drawable/eq_restore_bg"
        android:gravity="center"
        android:text="@string/eq_restore"
        android:textColor="@color/eq_restore_text"
        android:textSize="15sp" />
</LinearLayout>
