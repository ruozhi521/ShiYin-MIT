# 拾音播放器更新日志

## v1.27（2026-08-21）

**✨ 新增**
- **树形目录改版：逐级进入**！每个文件夹占一整行（左侧封面 + 右侧名称），点击进入二级界面，不再原地展开——大目录浏览终于清爽了
- 顶部新增**面包屑路径**（如 `‹ 音乐 > 华语`），点击或按返回键逐级回退
- **定时播放「仅播放一次」选项**：设置里勾选后只响一次、触发自动关闭；不勾选则每天同一时间自动播放

**🐛 修复**
- 修复歌词匹配错配：部分音声的标签标题带序号前缀（如 `02. 歌名`）时会错误匹配到其他文件夹的无关歌词——现在跨文件夹绝不匹配
- 修复拖动进度条时手指停住、时间跳回播放位置的问题——时间保持在你手指所指的位置
- 修复按住进度条时误触沉浸模式（进度条被隐藏）的问题
- 修复「每首歌进度记忆」播放中切歌再切回从头播、以及恢复后自动暂停/信息不刷新的问题
- 优化定时播放触发时的稳定性（闹钟冷启动播放卡顿的缓解）

**🛡️ 稳定性**
- 云端自动化测试持续护航，每次构建自动跑逻辑测试 + 模拟器界面测试

---

## v1.26（2026-08-20）

**✨ 新增**
- **b 站缓存 m4s 直接播放**：不用再转格式！缓存目录扫进去即播，自动识别视频标题（不再是数字文件名），同一视频只显示一条（自动过滤无声的视频流）
- **每首歌独立进度记忆**（可选开关，默认关闭）：听 A 到中途切到 B，再切回 A 从上次位置继续；整首听完自动清零，下次从头播
- **快进 / 快退 / 倍速**：播放页 -N 秒 / +N 秒按钮（时长可在设置里选 1~30 秒），倍速循环 1x→1.5x→2x→3x→4x，切歌自动复位

**🛡️ 稳定性**
- 播放失败自动切下一首（不再卡死在异常文件上）
- 时长不可读的文件（如部分 m4s）显示 `--:--`
- 云端自动化测试持续护航

---

## v1.25（2026-08-19）

**✨ 新增**
- **自定义快进退**：播放页「-N 秒 / +N 秒」按钮，时长可在设置里选（1 / 2 / 5 / 10 / 15 / 30 秒）
- **倍速播放**：播放页倍速按钮循环 1x → 1.5x → 2x → 3x → 4x，切歌自动恢复 1x

**🐛 修复**
- 修复同名不同格式歌曲（`歌名.mp3` + `歌名.flac`）歌词匹配错乱的问题
- 修复旧版本升级后偶发外置歌词匹配不到的问题

**🛡️ 稳定性**
- 引入自动化测试（27 项逻辑测试 + 模拟器界面测试），构建自动跑
- 音乐库布局可在设置里切换（网格大图标 / 树形目录）

---

## v1.24（2026-08-17）

**✨ 新增**
- 文件夹树形目录（Poweramp 式，设置里可切换）
- 音乐库布局切换（默认网格大图标）

**🐛 修复**
- 修复标签 title 与文件名不同的音频：外置歌词无法匹配、内嵌歌词读不到的问题
- 修复媒体通知进度一直显示 00:00/00:00 的问题
- 修复批量设置封面偶发不生效的问题
- 修复同名歌曲不同文件夹时歌词串行的问题
- 修复拖动进度条来回拖动卡顿的问题（改为松手才跳转）
- 修复部分设备选图器（Photo Picker）兼容性问题
- 清理冗余代码，提升稳定性
